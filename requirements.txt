# Versión fijada (no >=): el tag de la imagen base en el Dockerfile debe
# coincidir exacto con esta versión, o Playwright rechaza lanzar el
# navegador (ver comentario en el Dockerfile).
playwright==1.62.0
pytest>=8.0
pytest-playwright>=0.5
allure-pytest>=2.13
pandas>=2.2
openpyxl>=3.1
pytest-xdist>=3.6
requests>=2.32
pydantic>=2.8
pytest-rerunfailures>=14.0
